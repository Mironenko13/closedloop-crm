{
  "permissions": {
    "allow": [
      "Bash(powershell -Command \":*)"
    ]
  },
  "hooks": {
    "postToolUse": [{
      "matcher": "Edit|Write",
      "command": "npx eslint --quiet --no-warn-ignored $CLAUDE_FILE_PATHS 2>/dev/null && echo 'LINT OK' || echo 'LINT ERRORS — fix before continuing'"
    }]
  }
}
